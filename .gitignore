# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.backup

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code: per-user / transient state only.
# Tracked: .claude/plans, .claude/commands, .claude/skills (spec-kit installs).
.claude/settings.local.json
.claude/agents/
.claude/state/

# Spec-kit extension catalog cache (regenerated by `specify extension search`)
.specify/extensions/.cache/

claude.md

/playwright-report/
/test-results/
/tests/e2e/playwright-report/
/tests/e2e/test-results/

# Cargo workspace: only the repo-root Cargo.lock is tracked; ignore any
# per-member Cargo.lock that an `cd <member> && cargo` invocation might
# otherwise emit, and ignore all target/ build dirs. Per spec 001
# plan.md §IX.
target/
src-tauri/Cargo.lock
src/Cargo.lock
tools/build-themes/Cargo.lock

# Theme code-gen output (Phase 0 placeholder; regenerated by the
# Trunk pre-build hook from src/style/themes/*.css).
src/src/theme/themes.rs
