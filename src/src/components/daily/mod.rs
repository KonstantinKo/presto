// Daily drill-down view — Bundle B of feature 003.
//
// The view itself lands in T009 (GREEN of Phase 2's second TDD pair
// was already T007 for `day_clamp`; the view scaffold is the rest of
// Phase 2). This file currently registers the `day_clamp` submodule
// so the RED tests can compile; the `DailyView` component follows.

pub mod day_clamp;
